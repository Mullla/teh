- переписать функцию рендера на новый формат данных, эта функция не меняет исходные данные

- добавить функцию-шаблон новой даты со временем (?если дата есть, а времени нет, то добавить недостающее время и поставить статус isBusy = false) addRecord()

- добавить функцию, которая записывает данные из инпута в json с помощь функции-шаблона - файл и потом это отображается на странице (эта функция перезаписывает файл json)
- добавить слайдер, который листает расписание